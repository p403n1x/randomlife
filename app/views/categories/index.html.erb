<h1>Categories#index</h1>
<p>Find me in app/views/categories/index.html.erb</p>
<p>Here you can see a list of all the categories (hopefully!)</p>



<div class="container mt-5">
  <div class="row">
    <div class="col-md-6">
      <li>
        <% @categories.each do |category| %>
        <ul><% if category.kind == "task" %>
            <%= link_to category.title, new_category_task_path(category) %>
          <% else %>
            <%= link_to category.title, categories_path %>
          <% end %></ul>
        <% end %>
      </li>
      <%= link_to "Create new category", new_category_path(@category) %>

    </div>
  </div>
</div>


