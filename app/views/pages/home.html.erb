<div class="banner banner-home">
  <div class="container">

    <h1 id="banner-typed-text" class="mb-5"></h1>
    <br>
    <% if user_signed_in? %>
    <% else %>
    <strong><%= link_to 'Login', new_user_registration_path, class:'btn-home' %></strong>
    <% end %>
  </div>
</div>
